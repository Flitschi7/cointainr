# Cointainr Frontend Environment Configuration Example
# Copy this file to .env and adjust values as needed

# API Configuration
# ----------------
# Base URL for the backend API
VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1

# Cache Configuration
# ------------------
# Time-to-live for price cache in minutes (default: 15)
VITE_PRICE_CACHE_TTL_MINUTES=15
# Time-to-live for conversion cache in hours (default: 24)
VITE_CONVERSION_CACHE_TTL_HOURS=24

# Application Settings
# -------------------
# Default currency for display (default: USD)
VITE_DEFAULT_CURRENCY=USD
# Force refresh only mode - when true, always bypasses cache (default: false)
VITE_FORCE_REFRESH_ONLY=false